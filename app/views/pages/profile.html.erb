<div class="page-container">
  <%# <div class="profile-container"> %>
    <h1 class="page-title-s">Profile</h1>

    <div class="profile white-text">
      <%= image_tag "profile.png", alt: "Profile Picture", class: "profile-picture" %>
      <h2 class="username large-text"><%= current_user.username %></h2>
      <h3 class="small-text"><%= current_user.email %></h3>
      <h3 class="small-text">Active since · <%= current_user.created_at.strftime("%Y-%m-%d") %></h3>
    </div>

    <div class="profile-stats">
      <div class="profile-stat">
        <div class="large-text white-text">
          <%= @created_quizzes.count %>
        </div>
        <h2 class="small-text">Created Quizzes</h2>
        <!--
        <% @created_quizzes.each do |quiz| %>
          <p><%= quiz.title %></p>
        <% end %>
        -->
      </div>

      <div class="profile-stat">
        <div class="large-text white-text">
          <%= @taken_quizzes.count %>
        </div>
        <h2 class="small-text">Taken Quizzes</h2>
        <!--
        <% @taken_quizzes.each do |quiz| %>
          <p><%= quiz.title %></p>
        <% end %>
        -->
      </div>

      <div class="profile-stat">
          <div class="large-text text-white">
            <%= (@average_score*100).to_i %>%
            <%# <%= @average_score.round(2) %>
          </div>
        <h2 class="small-text">Average Score</h2>
      </div>
    </div>

    <div class="profile-buttons">
      <!--
      <%= link_to settings_path, class: "wide-button" do %>
        <%= image_tag("home-button.svg", alt: "Settings Icon", class: "button-icon") %>
        <span class="button-text">Settings</span>
      <% end %>
      -->
      <%= link_to edit_user_registration_path, class: "wide-button white-button" do %>
        <i class="fa-solid fa-lock button-icon"></i>
        <span class="button-text">Change Password</span>
      <% end %>

      <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: "wide-button white-button" do %>
        <i class="fa-solid fa-arrow-right-from-bracket button-icon"></i>
        <span class="button-text">Log Out</span>
      <% end %>
    </div>

  <%# </div> %>
</div>
